import { BconvertorPipe } from './bconvertor.pipe';

describe('BconvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new BconvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
